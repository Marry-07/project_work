version: "3.9"

services:
  db:
    image: postgres:15
    networks:
      - booking_network
    env_file:
      - ./db/.env
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      retries: 10
      timeout: 5s

  web:
    image: ghcr.io/<marry-07>/tour-backend:latest
    networks:
      - booking_network
    env_file:
      - ./db/.env

      # автоматический перезапуск контейнера, если он не поднят
    deploy:
      restart_policy:
        condition: on-failure

  frontend:
    image: ghcr.io/<marry-07>/tour-frontend:latest
    networks:
      - booking_network
    depends_on:
      - web
    deploy:
      restart_policy:
        condition: on-failure

  nginx:
    image: nginx:1.25
    networks:
      - booking_network
    ports:
      - "80:80"
    volumes:
    # меняем дефолт конфиг на собственный
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - web
    deploy:
      restart_policy:
        condition: on-failure

networks:
  booking_network:
    driver: overlay

volumes:
  pgdata: 
  # имя тома
